services:
  mqtt:
    image: eclipse-mosquitto
    ports:
      - "1883:1883"
      - "9001:9001"
    volumes:
      - ./docker/mqtt/:/mosquitto/config/:ro
  db:
    image: "mongo"
    ports:
      - "27017:27017"
    volumes:
      - ./docker/mongo/data/db:/data/db
